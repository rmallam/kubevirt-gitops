apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: 'test-logs'
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: openebs-kernel-nfs