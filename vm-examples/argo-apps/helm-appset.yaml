apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: virtualmachines-stretch-application-set
  namespace: openshift-gitops
spec:
  generators:
    - list:
        elements:
          - name: fedoravm
            valueFile: values-primary.yaml
            namespace: virtualmachines
  template:
    metadata:
      name: '{{name}}'
    spec:
      project: default
      source:
        helm:
          valueFiles:
          - values-common.yaml
          - '{{valueFile}}'
        repoURL: https://github.com/rmallam/kubevirt-gitops.git
        targetRevision: restructure
        path: 'vm-examples/helm/fedoravm'  # Path to your Helm chart
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{namespace}}'
      syncPolicy:
        syncOptions:
          - Validate=true
          - CreateNamespace=true
        automated:
          selfHeal: true
