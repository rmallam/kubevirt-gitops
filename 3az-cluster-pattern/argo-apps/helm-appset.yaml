apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: virtualmachines-stretch-application-set
  namespace: openshift-gitops
spec:
  generators:
    - list:
        elements:
          - name: primary-site
            valueFile: values-primary.yaml
            namespace: drtest
          - name: secondary-site
            valueFile: values-dr.yaml
            namespace: drtest
  template:
    metadata:
      name: '{{name}}'
    spec:
      project: multiaz
      source:
        helm:
          releaseName: multiaz-{{name}}
          valueFiles:
          - values-common.yaml
          - '{{valueFile}}'
        repoURL: https://github.com/rmallam/kubevirt-gitops.git
        targetRevision: restructure
        path: '3az-cluster-pattern/helm/stretchtest'  # Path to your Helm chart
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{namespace}}'
      syncPolicy:
        syncOptions:
          - Validate=true
          - CreateNamespace=true
